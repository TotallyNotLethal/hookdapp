# Capacitor Cordova Android Plugins

This directory is populated automatically by `npx cap sync android`. The repository keeps it as a placeholder so the Gradle
settings file can reference the module even before the sync command generates plugin sources and resources.

Run the sync command whenever you add or upgrade Capacitor or Cordova plugins:

```bash
npx cap sync android
```
